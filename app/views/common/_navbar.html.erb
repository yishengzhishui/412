<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="col-md-3">
      <div class="navbar-header">
        <a class="navbar-brand" href="/"><img src='<%= image_url 'logo.png' %>' >启航</a>
      </div>
    </div>




    <div class="col-md-6">
      <div class="nav navbar-nav">
        <%= form_tag search_jobs_path , :method => :get do %>
        <div class="input-group" id="search-id" >
          <input type="text" class="form-control" name="q" value="<%= params[:q] %>" placeholder="请输入职位信息">
          <div class="input-group-btn">
            <button class="btn btn-default" type="submit">
              <i class="glyphicon glyphicon-search"></i>
            </button>
          </div>
        </div>
        <% end %>
      </div>
    </div>



    <!-- Collect the nav links, forms, and other content for toggling-->
    <div class="col-md-3">
      <div class="collpase navbar-collapse" id="bs-exampel-navbar-collapse-1">
        <ul class="nav navbar-nav navbar-right log-word">
            <% if !current_user %>
              <li class="navbar-listItem"><%= button_to("注册", new_user_registration_path , method: :get,class: "btn navbar-btn btn-default") %> </li>
              <li class="navbar-listItem"><%= button_to("登录", new_user_session_path, method: :get,class: "btn navbar-btn btn-default") %></li>
            <% else %>
              <li class="dropdown">
                <a href="#" class="dropdown-togggle avatar" data-toggle="dropdown">
                  <img src= "<%= render_user_avatar(current_user, 30) %>">
                  <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to("个人中心", edit_account_user_path(current_user)) %></li>
                  <% if current_user.admin?%>
                    <li><%= link_to("已发布的职位", admin_jobs_path)%></li>
                  <% else %>
                    <li><%= link_to("已收藏的职位", account_jobs_path)%></li>
                  <% end %>
                  <li class="divider"><li>
                  <li><%= link_to("登出", destroy_user_session_path, method: :delete)%></li>

              </ul>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
    <!-- /.navbaar-colllapse-->
  </div>
  <!-- /.content-fluid-->
</nav>
